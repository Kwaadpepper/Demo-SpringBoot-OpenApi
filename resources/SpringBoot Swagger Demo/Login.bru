meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "login": "user.example",
    "password": "Password.1"
  }
}

settings {
  encodeUrl: true
}
